# Do 'msgfmt -c' check
PRESUBMIT_CHECK_MSGFMT = True

# Allow only files with 'charset=utf-8' in 'Content-Type' field
PRESUBMIT_CHECK_UTF8 = True

# Don't allow files with \r
PRESUBMIT_CHECK_DOS_NEWLINES = True

# Allow uploading only PO files
PRESUBMIT_CHECK_ALLOW_PO_ONLY = True