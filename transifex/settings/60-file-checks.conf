# These settings define behavior for uploaded file checks

# General Checks
FILECHECKS = {
    'DOS_NEWLINES': True,
    'UTF8': True,
}

# PO file reletad checks
FILECHECKS.update({
    'POFILE_MSGFMT': True,
})